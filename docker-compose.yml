services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: docnova
      POSTGRES_USER: docnova
      POSTGRES_PASSWORD: docnova_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
  
  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@docnova.local
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      - postgres

volumes:
  pgdata:
